const IService = require('../../../interfaces/base-service');

class ResultKeyService extends IService {
  /* eslint-disable no-useless-constructor */
  constructor(params) {
    super(params);
  }
}

module.exports = ResultKeyService;
