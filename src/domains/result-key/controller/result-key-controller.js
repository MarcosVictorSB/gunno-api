const IController = require('../../../interfaces/base-controller');

class ResultKeyController extends IController {
  /* eslint-disable no-useless-constructor */
  constructor(params) {
    super(params);
  }
}

module.exports = ResultKeyController;
