const IRepository = require('../../../interfaces/base-repository');

class ResultKeyRepository extends IRepository {
  /* eslint-disable no-useless-constructor */
  constructor(params) {
    super(params);
  }
}

module.exports = ResultKeyRepository;
